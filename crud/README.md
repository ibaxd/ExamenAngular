# Crud

## Cómo ejecutar el proyecto

```bash
git clone https://github.com/Samuel93SP/CrudAngular.git
cd crud
npm install --legacy-peer-deps
ng serve
```

Luego abre el navegador en `http://localhost:4200/`.
